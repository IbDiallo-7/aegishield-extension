// sidepanel.js
// This script's purpose is to load the main application module.
// Keeping it separate allows for flexibility if we need to add
// sidepanel-specific logic that shouldn't live in the main app bundle.

import '../assets/js/main.js';